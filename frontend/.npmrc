# Mesma trava do backend, e pelo mesmo motivo — ver `backend/.npmrc`.
#
# Os dois pacotes declaram `engines.node >= 24`, o CI roda em 24 e a Vercel
# também. Sem isto, uma máquina em Node 22 instala com um aviso e quebra
# depois, longe da causa.
engine-strict=true
