node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Ambiente — NUNCA versionar credenciais (lição do legado: config.php versionado)
.env
.env.local
.env.*.local
# Ambiente do e2e: o exemplo (.env.test.example) e versionado, o real nao.
.env.test
*.pem

# Uploads de usuário
backend/uploads/*
!backend/uploads/.gitkeep
# Arquivos escritos pelo e2e (UPLOAD_DIR do .env.test)
backend/uploads-test/

# Logs
*.log
npm-debug.log*
logs/

# Editores / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# Vinculo local com os projetos da Vercel (contem ids da conta).
.vercel

# graphify — grafo de conhecimento gerado localmente (artefato, regenerável)
graphify-out/

# Configuração pessoal do Claude Code. O `settings.json` é do PROJETO e sobe;
# preferência individual (plugin instalado, hook que depende de binário na
# máquina de quem escreveu) vai aqui — versionada, ela quebraria o clone de
# quem não tem a ferramenta, num hook que roda a cada chamada de ferramenta.
.claude/settings.local.json
.claude/skills/
